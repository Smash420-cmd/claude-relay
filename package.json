{
  "name": "claude-relay",
  "version": "0.4.20",
  "description": "Schedule prompts into local Claude Code sessions and auto-resume work across session limits.",
  "main": "main.js",
  "author": "Patrick",
  "license": "MIT",
  "bin": {
    "relay": "scripts/relay.js"
  },
  "scripts": {
    "start": "electron .",
    "build": "set ELECTRON_BUILDER_CACHE=.cache && electron-builder --win",
    "publish": "set ELECTRON_BUILDER_CACHE=.cache && electron-builder --win --publish always",
    "publish-retry": "node scripts/build-publish.js",
    "check": "node --check main.js && node --check preload.js && node --check src/paths.js && node --check src/store.js && node --check src/sessions.js && node --check src/executor.js && node --check src/scheduler.js && node --check src/tracker.js && node --check scripts/relay.js && node --check scripts/relay-statusline.js && node --check renderer/app.js"
  },
  "build": {
    "appId": "au.sojournly.relay",
    "productName": "Relay",
    "publish": {
      "provider": "github",
      "owner": "Smash420-cmd",
      "repo": "claude-relay"
    },
    "asarUnpack": ["scripts/**"],
    "win": {
      "target": "nsis",
      "icon": "assets/relay_tray/favicon.ico"
    },
    "nsis": {
      "oneClick": true,
      "runAfterFinish": true
    }
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^26.15.3"
  }
}
