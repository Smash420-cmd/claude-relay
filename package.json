{
  "name": "relay",
  "version": "0.1.0",
  "private": true,
  "description": "Schedule prompts into local Claude Code sessions and auto-resume work across session limits.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check src/paths.js && node --check src/store.js && node --check src/sessions.js && node --check src/executor.js && node --check src/scheduler.js && node --check renderer/app.js"
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^31.7.7"
  }
}
